# Локально — SQLite в папке backend
# DATABASE_URL=sqlite:///./autograd.db

# Продакшен на Vercel — обязательно внешняя БД (иначе записи не сохраняются между запросами).
# Бесплатный вариант: https://neon.tech → Connection string → PostgreSQL
# DATABASE_URL=postgresql://user:password@host.neon.tech/neondb?sslmode=require

# Публичный URL FastAPI (браузер и SSR)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Пароль входа в админку (только сервер — не префикс NEXT_PUBLIC)
ADMIN_PASSWORD=autograd2026

# Секрет подписи cookie сессии (смените в продакшене)
ADMIN_SESSION_SECRET=replace-with-long-random-string
